<Page
    x:Class="MindCanvas.Pages.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Loaded="EditorPage_V02Loaded">
    <Grid x:Name="EditorRoot" Background="{ThemeResource V4WindowBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="OutlineColumn" Width="306" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition x:Name="FormatColumn" Width="260" />
        </Grid.ColumnDefinitions>

        <Border
            x:Name="NarrowOutlinePane"
            Grid.Column="0"
            Background="{ThemeResource V4ContextActionsBackgroundBrush}"
            BorderBrush="{ThemeResource V4DividerStrongBrush}"
            BorderThickness="0,0,1,0"
            Padding="14,18,14,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid>
                    <TextBlock x:Name="OutlinePaneTitle" FontSize="20" FontWeight="Medium" Foreground="{ThemeResource V4TextStrongBrush}" />
                    <TextBlock Text="⋯" FontSize="22" HorizontalAlignment="Right" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                </Grid>
                <TextBox
                    x:Name="OutlineSearch"
                    Grid.Row="1"
                    Margin="0,12,0,12"
                    Height="38"
                    Background="{ThemeResource V4ControlBackgroundBrush}"
                    BorderBrush="{ThemeResource V4ControlStrokeBrush}" />
                <controls:TreeView
                    x:Name="OutlineTree"
                    Grid.Row="2"
                    Background="Transparent"
                    BorderThickness="0"
                    SelectionMode="Single" />
                <Button
                    x:Name="OutlineAddTopicButton"
                    Grid.Row="3"
                    HorizontalAlignment="Left"
                    HorizontalContentAlignment="Left"
                    Padding="0,10,0,14"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{ThemeResource V4TextStrongBrush}" />
            </Grid>
        </Border>

        <Border
            x:Name="WideOutlinePane"
            Grid.Column="1"
            Background="{ThemeResource V4WindowBackgroundBrush}"
            Visibility="Collapsed">
            <Grid Padding="22,22,22,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Spacing="6">
                    <TextBlock x:Name="WideOutlineTitle" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource V4TextStrongBrush}" />
                    <TextBlock x:Name="WideOutlineCount" FontSize="12" Foreground="{ThemeResource V4TextMutedBrush}" />
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,18,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        x:Name="WideOutlineSearch"
                        Width="520"
                        Height="38"
                        HorizontalAlignment="Left"
                        Background="{ThemeResource V4ControlBackgroundBrush}"
                        BorderBrush="{ThemeResource V4ControlStrokeBrush}" />
                    <Border
                        Grid.Column="1"
                        Width="264"
                        Height="38"
                        BorderBrush="{ThemeResource V4DividerStrongBrush}"
                        BorderThickness="1"
                        Background="{ThemeResource V4ControlBackgroundBrush}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
                            <TextBlock x:Name="WideOutlineDensityLabel" FontSize="13" Foreground="{ThemeResource V4TextSecondaryBrush}" VerticalAlignment="Center" />
                            <Button x:Name="CompactDensityButton" Height="30" MinWidth="70" Background="{ThemeResource V4ControlSelectedBackgroundBrush}" Foreground="{ThemeResource V4AccentForegroundBrush}" />
                            <Button x:Name="ComfortableDensityButton" Height="30" MinWidth="74" Background="{ThemeResource V4ControlBackgroundBrush}" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,18,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="260" />
                        <ColumnDefinition Width="116" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="WideOutlineTopicHeader" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                    <TextBlock x:Name="WideOutlineNoteHeader" Grid.Column="1" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                    <TextBlock x:Name="WideOutlineStatusHeader" Grid.Column="2" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource V4TextMutedBrush}" />
                    <Border Grid.Column="0" Grid.ColumnSpan="3" Height="1" Margin="0,8,0,0" Background="{ThemeResource V4DividerStrongBrush}" VerticalAlignment="Bottom" />
                </Grid>

                <ScrollViewer Grid.Row="3" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="WideOutlineRows" Spacing="10" />
                </ScrollViewer>
            </Grid>
        </Border>

        <Grid x:Name="MapHost" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="36" />
            </Grid.RowDefinitions>
            <ScrollViewer
                x:Name="MapScrollViewer"
                HorizontalScrollMode="Enabled"
                VerticalScrollMode="Enabled"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto"
                ZoomMode="Enabled"
                MinZoomFactor="0.25"
                MaxZoomFactor="4"
                ViewChanged="MapScrollViewer_ViewChanged">
                <Canvas
                    x:Name="MapCanvas"
                    Width="1600"
                    Height="1000"
                    Background="{ThemeResource V4CardBackgroundBrush}" />
            </ScrollViewer>
            <Border
                Grid.Row="1"
                Background="{ThemeResource V4ContextActionsBackgroundBrush}"
                BorderBrush="{ThemeResource V4CanvasGridLineBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,0">
                <Grid>
                    <TextBlock x:Name="MapStatusText" FontSize="11" Foreground="{ThemeResource V4TextSecondaryBrush}" VerticalAlignment="Center" />
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
                        <Button x:Name="ZoomOutButton" Width="28" Height="28" Padding="0" Content="−" Click="ZoomOutButton_Click" />
                        <TextBlock x:Name="ZoomText" Width="48" Text="100%" TextAlignment="Center" FontSize="11" Foreground="{ThemeResource V4TextSecondaryBrush}" VerticalAlignment="Center" />
                        <Button x:Name="ZoomInButton" Width="28" Height="28" Padding="0" Content="＋" Click="ZoomInButton_Click" />
                        <Button x:Name="FitViewButton" MinWidth="52" Height="28" Padding="8,0" Click="FitViewButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border
            x:Name="FormatPanel"
            Grid.Column="2"
            Background="{ThemeResource V4ContextActionsBackgroundBrush}"
            BorderBrush="{ThemeResource V4DividerStrongBrush}"
            BorderThickness="1,0,0,0"
            Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock x:Name="FormatPanelTitle" FontSize="20" FontWeight="Medium" Foreground="{ThemeResource V4TextStrongBrush}" />
                        <Button x:Name="CollapseFormatButton" Width="32" Height="32" HorizontalAlignment="Right" Background="{ThemeResource V4ControlBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" Foreground="{ThemeResource V4TextSecondaryBrush}" Click="CollapseFormatButton_Click" />
                    </Grid>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <ToggleButton x:Name="StructureTabButton" Width="108" Height="34" IsChecked="True" Background="{ThemeResource V4ControlSelectedBackgroundBrush}" BorderBrush="{ThemeResource V4ControlSelectedStrokeBrush}" Foreground="{ThemeResource V4AccentForegroundBrush}" />
                        <ToggleButton x:Name="ThemeTabButton" Width="108" Height="34" Background="{ThemeResource V4ControlBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" Foreground="{ThemeResource V4TextPrimaryBrush}" />
                    </StackPanel>

                    <TextBlock x:Name="CurrentTemplateLabel" FontSize="12" FontWeight="Medium" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                    <Border
                        Height="86"
                        CornerRadius="8"
                        Padding="12"
                        Background="{ThemeResource V4CardBackgroundBrush}"
                        BorderBrush="{ThemeResource V4CardStrokeBrush}"
                        BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="54" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border
                                Width="42"
                                Height="34"
                                CornerRadius="4"
                                Background="{ThemeResource V4StatusProgressBackgroundBrush}"
                                VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock x:Name="CurrentTemplateName" FontSize="13" FontWeight="Medium" Foreground="{ThemeResource V4TextStrongBrush}" />
                                <TextBlock x:Name="CurrentTemplateHint" FontSize="11" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                            </StackPanel>
                            <Button x:Name="ChangeTemplateButton" Grid.Column="2" MinWidth="50" Height="34" VerticalAlignment="Center" Background="{ThemeResource V4ControlBackgroundBrush}" BorderBrush="{ThemeResource V4DividerStrongBrush}" Foreground="{ThemeResource V4TextStrongBrush}" />
                        </Grid>
                    </Border>

                    <TextBlock x:Name="StructureLabel" FontSize="12" FontWeight="Medium" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="66" />
                            <RowDefinition Height="66" />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Grid.Column="0" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlSelectedStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border Width="36" Height="18" Background="{ThemeResource V4NodeAccentBlueBrush}" CornerRadius="3" />
                                <TextBlock x:Name="StructureMindMapText" FontSize="10" Foreground="{ThemeResource V4AccentStrongBrush}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="0" Grid.Column="1" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border Width="36" Height="18" Background="{ThemeResource V4PreviewBranchBrush}" CornerRadius="3" />
                                <TextBlock x:Name="StructureLogicText" FontSize="10" Foreground="{ThemeResource V4TextStrongBrush}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="0" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border Width="36" Height="18" Background="{ThemeResource V4PreviewBranchBrush}" CornerRadius="3" />
                                <TextBlock x:Name="StructureTreeText" FontSize="10" Foreground="{ThemeResource V4TextStrongBrush}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border Width="36" Height="18" Background="{ThemeResource V4PreviewBranchBrush}" CornerRadius="3" />
                                <TextBlock x:Name="StructureOrgText" FontSize="10" Foreground="{ThemeResource V4TextStrongBrush}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TextBlock x:Name="ThemeLabel" FontSize="12" FontWeight="Medium" Foreground="{ThemeResource V4TextSecondaryBrush}" />
                    <Grid ColumnSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="4">
                                <Border Width="40" Height="30" Background="#EBF5FF" />
                                <TextBlock x:Name="ThemeFluentText" FontSize="9" Foreground="{ThemeResource V4TextSecondaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="4">
                                <Border Width="40" Height="30" Background="#FFEBF0" />
                                <TextBlock x:Name="ThemeSoftText" FontSize="9" Foreground="{ThemeResource V4TextSecondaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="4">
                                <Border Width="40" Height="30" Background="#EDEDED" />
                                <TextBlock x:Name="ThemeMonoText" FontSize="9" Foreground="{ThemeResource V4TextSecondaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="3" CornerRadius="6" Background="{ThemeResource V4CardBackgroundBrush}" BorderBrush="{ThemeResource V4ControlStrokeBrush}" BorderThickness="1">
                            <StackPanel Spacing="4" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="4">
                                <Border Width="40" Height="30" Background="#2E2E30" />
                                <TextBlock x:Name="ThemeDarkText" FontSize="9" Foreground="{ThemeResource V4TextSecondaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TextBlock x:Name="FormatPanelHint" FontSize="10" Foreground="{ThemeResource V4TextSecondaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>